# Dashboard_Arrecadacao_RENAINF
script em, linguagem R para criação de um dashboard de Arrecadação sobre RENAINF.

para ler a tabela na GUI do vscode:

library(readxl)
dados <- read_excel("Banco_RENAINF.xlsx")      
View(dados)

